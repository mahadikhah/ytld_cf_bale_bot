name: Mirror Documentation Site (SingleFile)

on:
  workflow_dispatch:
    inputs:
      chat_id:
        description: 'Bale chat ID'
        required: true
      docs_url:
        description: 'URL of the docs (can be a leaf page)'
        required: true
      title:
        description: 'Site name (for zip filename)'
        default: 'docs'
      capture_timeout_minutes:
        description: 'Max capture time (minutes)'
        default: '5'

jobs:
  mirror-and-send:
    runs-on: ubuntu-latest
    timeout-minutes: 10            # hard upper bound (capture + zip + upload)

    steps:
      - uses: actions/checkout@v4

      - name: Cache Puppeteer
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/puppeteer
            ~/.npm
          key: ${{ runner.os }}-puppeteer-${{ hashFiles('.github/workflows/getdocs.yml') }}
          restore-keys: |
            ${{ runner.os }}-puppeteer-

      - name: Setup Node & install dependencies
        run: |
          npm init -y
          npm install puppeteer single-file-cli

      - name: Discover pages and capture (time‑limited)
        id: capture
        continue-on-error: true
        env:
          DOCS_URL: ${{ inputs.docs_url }}
          TITLE: ${{ inputs.title }}
          CAPTURE_TIMEOUT_MINUTES: ${{ inputs.capture_timeout_minutes }}
        run: |
          mkdir -p output

          cat > capture.js << 'SCRIPT_END'
          const puppeteer = require('puppeteer');
          const { execSync } = require('child_process');
          const fs = require('fs');
          const path = require('path');

          // ---- config ----
          const baseUrl = process.env.DOCS_URL;
          const baseUrlObj = new URL(baseUrl);
          const origin = baseUrlObj.origin;

          // Decide the documentation root path
          let basePath = baseUrlObj.pathname;
          // If it looks like a leaf page (no trailing slash and last segment doesn't contain a dot),
          // go up one directory to capture the whole section
          if (!basePath.endsWith('/')) {
              const parts = basePath.split('/');
              const last = parts[parts.length - 1];
              if (!last.includes('.')) {
                  basePath = parts.slice(0, -1).join('/') + '/';
              }
          }
          if (!basePath.endsWith('/')) basePath += '/';

          const timeoutMinutes = parseInt(process.env.CAPTURE_TIMEOUT_MINUTES || '5', 10);
          const MAX_TIME_MS = timeoutMinutes * 60 * 1000;
          const startTime = Date.now();
          let stoppedEarly = false;

          function timeOver() {
            return (Date.now() - startTime) >= MAX_TIME_MS;
          }

          function safeName(url) {
            return url
              .replace(/^https?:\/\//, '')
              .replace(/[^a-zA-Z0-9\/\-]/g, '_')
              .replace(/\//g, '_')
              .substring(0, 80) + '.html';
          }

          async function collectLinks(page, origin, pathPrefix) {
            return await page.evaluate(({ origin, pathPrefix }) => {
              const anchors = document.querySelectorAll('a[href]');
              const urls = new Set();
              anchors.forEach(a => {
                try {
                  const href = a.href;
                  if (!href || !href.startsWith(origin)) return;
                  const hrefPath = new URL(href).pathname;
                  if (!hrefPath.startsWith(pathPrefix)) return;
                  const clean = href.split('#')[0].replace(/\/$/, '');
                  urls.add(clean);
                } catch(e) {}
              });
              urls.add(origin + pathPrefix.replace(/\/$/, ''));
              return Array.from(urls).slice(0, 30);
            }, { origin, pathPrefix });
          }

          async function run() {
            console.log(`Documentation root: ${origin}${basePath}`);
            console.log(`Time limit: ${timeoutMinutes} minute(s)`);

            console.log('Launching browser...');
            const browser = await puppeteer.launch({
              headless: 'new',
              args: ['--no-sandbox', '--disable-setuid-sandbox']
            });
            const page = await browser.newPage();

            try {
              console.log(`Loading base page: ${baseUrl}`);
              await page.goto(baseUrl, { waitUntil: 'networkidle2', timeout: 30000 });

              let links = await collectLinks(page, origin, basePath);

              // If too few links, try to find a child page that has the full navigation
              if (links.length <= 2) {
                const altLink = links.find(l => l !== origin + basePath.replace(/\/$/, ''));
                if (altLink) {
                  console.log(`Too few links – jumping to ${altLink} for navigation`);
                  await page.goto(altLink, { waitUntil: 'networkidle2', timeout: 30000 });
                  links = await collectLinks(page, origin, basePath);
                }
              }

              console.log(`Found ${links.length} pages`);
              fs.writeFileSync('urls.txt', links.join('\n'));

              let captured = 0;
              for (const url of links) {
                if (timeOver()) {
                  stoppedEarly = true;
                  console.log('Time limit reached – stopping capture.');
                  break;
                }

                const filename = safeName(url);
                const outputPath = path.join('output', filename);

                if (fs.existsSync(outputPath)) {
                  console.log(`Skipping: ${filename}`);
                  continue;
                }

                try {
                  console.log(`[${captured+1}/${links.length}] Capturing: ${url}`);
                  execSync(
                    `npx single-file "${url}" "${outputPath}"` +
                    ` --browser-arg="--no-sandbox"` +
                    ` --browser-arg="--disable-setuid-sandbox"` +
                    ` --browser-load-max-time=15000` +
                    ` --browser-capture-max-time=15000` +
                    ` --browser-wait-until="networkIdle"`,
                    { stdio: 'pipe', timeout: 20000 }
                  );
                  captured++;
                  console.log(`  -> saved as ${filename}`);
                } catch(e) {
                  console.error(`  -> failed: ${e.message?.slice(0,100)}`);
                }
              }
            } finally {
              await browser.close();
            }

            const files = fs.readdirSync('output').filter(f => f.endsWith('.html'));
            let indexHtml = '<!DOCTYPE html><html><head><meta charset="UTF-8"><title>Docs Mirror</title></head><body><h1>Documentation</h1><ul>';
            files.forEach(f => {
              if (f === 'index.html') return;
              indexHtml += `<li><a href="${f}">${f.replace('.html','').replace(/_/g,' ')}</a></li>`;
            });
            indexHtml += '</ul></body></html>';
            fs.writeFileSync('output/index.html', indexHtml);
            console.log(`Written index.html – total pages: ${files.length}`);
            if (stoppedEarly) console.log('Capture stopped early due to time limit.');
          }

          run().catch(e => {
            console.error('Capture script error:', e);
            process.exit(1);
          });
          SCRIPT_END

          node capture.js
          EXIT_CODE=$?
          echo "Capture exited with code $EXIT_CODE"
          # If it failed for a reason other than time limit, we'll still try to zip what we have.

      - name: Zip and send to user
        env:
          BALE_BOT_TOKEN: ${{ secrets.BALE_BOT_TOKEN }}
          CHAT_ID: ${{ inputs.chat_id }}
          DOCS_URL: ${{ inputs.docs_url }}
          TITLE: ${{ inputs.title }}
          WORKER_URL: ${{ secrets.WORKER_URL }}
          WORKER_SECRET: ${{ secrets.WORKER_SECRET }}
        run: |
          SAFE_NAME=$(echo "$DOCS_URL" | sed -E 's#^https?://##; s/[^a-zA-Z0-9._-]/_/g; s/__+/_/g' | cut -c1-50)
          [ -z "$SAFE_NAME" ] && SAFE_NAME=$(echo "$TITLE" | tr -d '\\/*?:"<>|' | cut -c1-40)
          ZIP_NAME="${SAFE_NAME}.zip"

          send_message() {
            curl -s -X POST "https://tapi.bale.ai/bot${BALE_BOT_TOKEN}/sendMessage" \
                 -H "Content-Type: application/json" \
                 -d "{\"chat_id\": \"${CHAT_ID}\", \"text\": \"$1\", \"parse_mode\": \"Markdown\"}"
          }

          if [ ! -d output ] || [ -z "$(ls -A output/*.html 2>/dev/null)" ]; then
            send_message "❌ No pages were captured. The site might be too heavy or unreachable."
            exit 0
          fi

          # Create zip if not already created by the capture step
          if [ ! -f "$ZIP_NAME" ]; then
            cd output
            zip -r "../${ZIP_NAME}" .
            cd ..
          fi

          ZIP_SIZE=$(stat -c%s "$ZIP_NAME")
          MAX_SIZE=$((20 * 1024 * 1024))

          try_upload() {
            local file="$1"; local caption="$2"
            for attempt in 1 2; do
              if [ -n "$caption" ]; then
                RESPONSE=$(curl -s -w "%{http_code}" -F "document=@$file" -F "caption=$caption" \
                           "https://tapi.bale.ai/bot${BALE_BOT_TOKEN}/sendDocument?chat_id=${CHAT_ID}")
              else
                RESPONSE=$(curl -s -w "%{http_code}" -F "document=@$file" \
                           "https://tapi.bale.ai/bot${BALE_BOT_TOKEN}/sendDocument?chat_id=${CHAT_ID}")
              fi
              HTTP_CODE=$(echo "$RESPONSE" | tail -c 4)
              BODY=$(echo "$RESPONSE" | head -c -4)
              if [ "$HTTP_CODE" = "200" ] && echo "$BODY" | grep -q '"ok":true'; then
                echo "Upload successful on attempt $attempt"
                return 0
              fi
              [ $attempt -lt 2 ] && sleep 1
            done
            return 1
          }

          if [ "$ZIP_SIZE" -le "$MAX_SIZE" ]; then
            echo "Zip size $(($ZIP_SIZE / 1024 / 1024)) MB – sending directly"
            if try_upload "$ZIP_NAME" "📚 ${SAFE_NAME}"; then
              PAGES=$(unzip -Z1 "$ZIP_NAME" | grep -c '\.html$')
              send_message "✅ *Docs captured:* \`$ZIP_NAME\` ($PAGES pages)\n\n📱 Extract and open \`index.html\` to navigate offline."
            else
              send_message "❌ Bale unstable – upload failed."
            fi
          else
            echo "Archive too large – splitting..."
            # Split the existing zip. This creates SAFE_NAME.z01, SAFE_NAME.z02, ..., SAFE_NAME.zip (the last one)
            zip -s 15m "${ZIP_NAME}" --out "${SAFE_NAME}_split.zip" || true
            # Remove original large zip and rename split files
            rm -f "$ZIP_NAME"
            PARTS=$(ls "${SAFE_NAME}_split".z* "${SAFE_NAME}_split".zip 2>/dev/null | sort -V)
            # If split failed, try a different approach: split the original and rename
            if [ -z "$PARTS" ]; then
              zip -s 15m "${ZIP_NAME}" --out "${SAFE_NAME}_split.zip" || {
                send_message "❌ Failed to split the archive."
                exit 0
              }
              rm -f "$ZIP_NAME"
              PARTS=$(ls "${SAFE_NAME}_split".z* "${SAFE_NAME}_split".zip 2>/dev/null | sort -V)
            fi

            if [ -z "$PARTS" ]; then
              send_message "❌ Splitting failed – please try a smaller section or reduce capture time."
              exit 0
            fi

            for part in $PARTS; do
              echo "Uploading part: $part"
              if ! try_upload "$part"; then
                send_message "❌ Bale interrupted – could not send all parts."
                exit 0
              fi
              sleep 1
            done
            send_message "✅ *Docs captured (split archive).* Extract the files and open \`index.html\`."
          fi
          
      - name: Unlock worker queue
        if: always()
        env:
          WORKER_URL: ${{ secrets.WORKER_URL }}
          WORKER_SECRET: ${{ secrets.WORKER_SECRET }}
          CHAT_ID: ${{ inputs.chat_id }}
        run: |
          if [ -n "$WORKER_URL" ] && [ -n "$WORKER_SECRET" ]; then
            curl -s -X POST "$WORKER_URL/github/done" \
                 -H "Content-Type: application/json" \
                 -d "{\"secret\": \"$WORKER_SECRET\", \"chat_id\": \"$CHAT_ID\"}"
          fi
